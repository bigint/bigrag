{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/api/Dockerfile",
    "watchPatterns": [
      "/api/**",
      "/railway/worker.json",
      "/railway/variables/worker.env"
    ]
  },
  "deploy": {
    "startCommand": "bigrag-worker --processes ${BIGRAG_WORKER_PROCESSES:-1} --threads ${BIGRAG_WORKER_THREADS:-8}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "drainingSeconds": 30
  }
}
