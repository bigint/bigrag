# bigRAG server base URL (the FastAPI service).
BIGRAG_URL=http://localhost:4000

# Cookie configuration — set to "true" when serving the Studio over HTTPS.
# When unset, cookies work on plain http://localhost for development.
# BIGRAG_SESSION_COOKIE_SECURE=true
