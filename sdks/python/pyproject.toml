[project]
name = "bigrag"
version = "0.2.0"
description = "Python client for bigRAG RAG platform"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
