[package]
name = "rag-computer"
version = "2026.5.7"
edition = "2021"
rust-version = "1.75"
description = "Rust client for rag.computer — a self-hostable RAG platform"
license = "MIT"
repository = "https://github.com/yoginth/rag-computer"
documentation = "https://docs.rs/rag-computer"
keywords = ["rag", "vector-search", "embeddings", "ai", "document-ingestion"]
categories = ["api-bindings", "web-programming::http-client"]

[dependencies]
reqwest = { version = "0.13", features = ["json", "multipart", "query", "stream"] }
tokio = { version = "1", features = ["fs", "macros", "rt-multi-thread", "time"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
futures-core = "0.3"
bytes = "1"

[dev-dependencies]
httpmock = "0.8"
