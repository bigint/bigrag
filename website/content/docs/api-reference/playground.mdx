---
title: Playground
description: Legacy session-only playground generation endpoint.
---

Base path: `/v1/playground`

Session-only. This is the legacy low-level generation endpoint. New applications should use the production [`/v1/chat`](/docs/api-reference/chat) API, which performs retrieval on the backend, stores conversations, and returns citations.

## Stream Chat

```
POST /v1/playground/chat
```

Streams an OpenAI chat completion as Server-Sent Events.

```json
{
  "model": "gpt-4o-mini",
  "temperature": 0.2,
  "messages": [
    { "role": "system", "content": "Use the supplied context." },
    { "role": "user", "content": "What changed in the policy?" }
  ]
}
```

SSE frames use this shape:

```text
data: {"delta":"partial text"}

data: [DONE]
```

If the upstream OpenAI request fails after streaming starts, the endpoint emits:

```text
data: {"error":"OpenAI error message"}
```
