---
title: Admin UI
description: Setup, collections, chat, API keys, usage, audit, webhooks, backups, and settings.
---

import { Callout } from "fumadocs-ui/components/callout";

The admin UI is the Vite + TanStack Router single-page app packaged with bigRAG. Every control is also a REST API, so the admin UI is optional — you can run bigRAG headless — but it's the fastest way to bootstrap, prototype, and operate the platform.

The bundled interface uses normal-weight body text with slightly restrained emphasis weights so dense admin screens stay readable without feeling visually heavy.

## Running the Admin UI

```bash
pnpm dev:app        # admin UI on http://localhost:3000
```

The admin UI calls the FastAPI backend directly. The default API URL is `http://localhost:4000`; override it with `VITE_BIGRAG_URL` at build/dev time or `BIGRAG_URL` in the app container. Configure browser origins in `/settings → Security` after the API has its bootstrap database wiring.

For cross-site production deployments, enable secure session cookies and usually set `SameSite=none` in `/settings → Security`. Same-site subdomain deployments can usually keep `SameSite=lax`.

## Route map

| Path | Purpose |
|------|---------|
| `/setup` | One-time page to create the first admin account. Disappears once `needs_setup: false`. |
| `/login` | Email + password login for admins and members. |
| `/overview` | Platform-wide stats — collection count, document states, stored token count, queue depth, readiness. |
| `/collections` | List, create, delete, and search collections. |
| `/collections/[name]` | Redirects to the collection's documents tab. |
| `/collections/[name]/documents` | Upload files or folders through resumable upload sessions, list, inspect status, and delete documents. |
| `/collections/[name]/documents/[docId]` | Document detail — chunk viewer, SSE progress, failed-ingest error, reprocess, and delete. |
| `/collections/[name]/connectors` | Redirects to the collection's connector tabs. |
| `/collections/[name]/connectors/google-drive` | Browse Google Drive, sync selected files/folders, monitor Drive sync progress, trigger manual resync, and manage sync schedules. |
| `/collections/[name]/search` | Query tester scoped to the collection — tweak `search_mode`, `top_k`, and rerank when enabled. |
| `/collections/[name]/settings` | Edit description, metadata, metadata schema, reranking, chunking, and default query params. |
| `/chat` | Production chat UI; retrieve from one collection, stream cited answers through the backend, and continue stored conversations. |
| `/models` | Manage [embedding presets](/docs/api-reference/embedding-presets) — reusable provider + model + key tuples. |
| `/mcp` | Mint, scope, rotate, and revoke [MCP server](/docs/api-reference/mcp-servers) credentials for Claude Desktop / Cursor / custom MCP clients. |
| `/api-keys` | Mint, scope, and revoke `bigrag_sk_…` keys. Plaintext shown once at creation. |
| `/access-logs` | Access-log explorer for query, vector, chat, and evaluation traffic. |
| `/usage` | Usage and cost explorer for requests, indexed data, tokens, and estimated embedding spend. |
| `/audit` | Immutable admin audit trail with actor, action, resource, IP, and timestamp details. |
| `/webhooks` | Register, test, and delete webhooks. Delivery history and replay are available through the API. |
| `/backups` | Configure readable backup storage, start full-instance exports, and monitor backup history. |
| `/settings` | Operator-console settings workspace for account, platform, data, runtime, and connector sections. |
| `/evals` | Run golden-set retrieval evaluations. |

Unknown admin routes render the built-in 404 screen with links back to overview and collections. Unexpected route render failures use the admin 500 screen with a retry action and an overview fallback.

## Settings sections

The `/backups` page combines backup destination settings with `GET /v1/admin/backups`, `GET /v1/admin/realtime/backups`, and `POST /v1/admin/backups` so backup operations stay out of the general Settings workspace. Usage and audit also live as first-class sidebar pages at `/usage` and `/audit`.

The settings page is a task-oriented operator workspace. It groups the old runtime registry into six areas: Account, Health, Security, Data, Models, and Connectors. Common controls appear first with a recommended next action; raw setting keys, source metadata, live-apply markers, and rarely changed limits stay inside each area's Advanced controls.

Existing deep links keep working. Legacy values such as `/settings?tab=server`, `/settings?tab=vector_store`, `/settings?tab=storage`, `/settings?tab=search`, and `/settings?tab=chat` open the matching redesigned area while focusing the relevant runtime panel.

Older `/settings?tab=backups`, `/settings?tab=usage`, and `/settings?tab=audit` links redirect to `/backups`, `/usage`, and `/audit`.

| Group | Section | Reads from | Writes to |
|-------|---------|-----------|-----------|
| **Personal** | Account | `GET /v1/auth/me` | `POST /v1/auth/password`, `POST /v1/auth/logout-all` |
| **Operate** | Health | `GET /health/ready`, `GET /v1/admin/realtime/platform/readiness` | — |
| **Operate** | Security | `GET /admin/settings` | `PUT /admin/settings`, `POST /admin/settings/test`, `POST /admin/settings/reset`, `POST /admin/settings/embedding-cache/purge` |
| **Operate** | Data | `GET /admin/settings` | `PUT /admin/settings`, `POST /admin/settings/test`, `POST /admin/settings/reset` |
| **Operate** | Models | `GET /admin/settings` | `PUT /admin/settings`, `POST /admin/settings/test`, `POST /admin/settings/reset` |
| **Integrate** | Connectors | `GET /v1/admin/connectors/{provider}`, `GET /v1/connectors/{provider}/account` | `PUT /v1/admin/connectors/{provider}`, `POST /v1/connectors/{provider}/disconnect` |

## Realtime updates

The admin UI uses session-cookie [admin realtime SSE streams](/docs/api-reference/admin-realtime) for live document progress, batch upload progress, collection stats, connector sync state, access logs, audit log, usage, platform stats, and readiness. Each stream sends an immediate full `snapshot` payload that matches the related REST response, then sends later snapshots as data changes.

After a large local upload starts, the collection documents page opens `GET /v1/admin/realtime/collections/{name}/upload-sessions/{session_id}`. The panel shows received, queued, ingesting, completed, failed, and canceled counts plus a small recent item window, so 10,000-file uploads do not require a 10,000-ID status stream.

The Google Drive connector page opens `GET /v1/admin/realtime/google/sync-jobs?collection={name}` beside the source stream. The sync monitor shows Drive scanning, download/update progress, remote deletion cleanup, and queued-for-ingestion completion; document conversion and indexing continue through the normal document progress surfaces.

Upload progress restoration, the current Drive browser selection, and in-progress chat turns are browser-local admin UI state. Upload session IDs are persisted in local storage so a collection's upload panel can reconnect after navigation; transient chat and Drive browser state are not part of the REST API.

REST document status endpoints remain available for API clients and SDKs that prefer polling. `GET /v1/collections/{name}/events` remains available for collection-wide ingestion events outside the admin UI.

## Chat specifics

The chat UI sends each turn to `POST /v1/chat`. Retrieval, prompt assembly, provider calls, citations, and conversation persistence all happen on the backend. The saved OpenAI key stays on the backend; preference responses only expose whether a key is configured. Preferences such as model, temperature, `top_k`, search mode, rerank preference, and system prompt persist via `/v1/auth/preferences`.

### Citations

Retrieved chunks carry citation provenance — `document_id`, `document_filename`, `chunk_index`, and Docling-derived `page_no` / `char_start` / `char_end` (see [`ChatSource`](/docs/api-reference/chat)). The backend hands every chunk to the LLM as `[N] (filename, page X) <text>` and instructs the model to cite with bracketed numbers. In the rendered answer, every `[N]` token is a clickable link that expands the **View sources** panel and scrolls to the matching source card; the card itself shows the document filename, chunk index, page badge, and character offsets when present.

<Callout type="info">
The admin UI uses session cookies exclusively. To automate anything the admin UI does, mint an API key at `/api-keys` and use it with one of the [SDKs](/docs/sdks/typescript) — or with raw HTTP using the same REST endpoints.
</Callout>

## When to skip the Admin UI

- **Pure backend deployment** — no admin UI process, everything via API keys and SDKs. Set `BIGRAG_ENV=prod` and use [`POST /v1/auth/setup`](/docs/api-reference/authentication) from an admin workstation to bootstrap.
- **Embedded experiences** — if you're surfacing search inside your own app, skip admin UI and build against the [query API](/docs/api-reference/query).
- **Read-only operators** — create a `member` account; they get `/overview`, `/collections` (read), and `/chat` but can't mutate.
