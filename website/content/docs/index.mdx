---
title: Introduction
description: bigRAG is an open-source, self-hostable RAG platform for document ingestion, vector search, and retrieval-augmented generation.
full: true
---

import { Cards, Card } from "fumadocs-ui/components/card";
import { Mermaid } from "@/components/mermaid";

bigRAG is an open-source RAG (Retrieval-Augmented Generation) platform. It provides a complete pipeline for document ingestion, chunking, embedding, and vector search — behind a REST API, TypeScript / Python SDKs, an admin UI, and an MCP server for Claude Desktop, Cursor, and any MCP-aware runtime.

## Key Features

- **End-to-end RAG pipeline** — upload documents, auto-chunk, embed, search, and rerank in one platform
- **Any document format** — PDF, DOCX, PPTX, XLSX, HTML, Markdown, CSV/TSV/XML/JSON, and scanned images (OCR) via [Docling](https://github.com/DS4SD/docling)
- **Any embedding model** — OpenAI, Cohere, Voyage, or `openai_compatible` gateways (Ollama, vLLM, TEI, LiteLLM, Azure, Bedrock) configured per collection
- **Three search modes** — semantic, keyword, and hybrid, with optional Cohere reranking
- **Turbopuffer vector search** — semantic, keyword, and hybrid retrieval with namespace isolation
- **First-class auth** — admin accounts, session cookies, scoped `bigrag_sk_…` API keys, and full audit/access logs
- **Operational features** — batch ingestion, Google Drive sync, webhooks with HMAC signatures, metadata-schema validation, an evaluation runner, and per-collection query analytics
- **Admin UI** — first-run setup, collection browser, stateless chat, connector management, access logs, MCP credentials, and API-key minting
- **Self-hostable control plane** — Docker Compose for the API, admin UI, Postgres, and Redis
- **MIT licensed** — run it anywhere, forever free

## Architecture

<Mermaid title="bigRAG platform architecture" chart='flowchart LR
    Clients["Client surfaces<br/>Admin UI / SDKs / MCP"] -->|"session cookies or API keys"| API["bigRAG API<br/>FastAPI"]

    API --> Control["Control plane<br/>auth / collections / webhooks / audit"]
    API --> Ingestion["Ingestion plane<br/>documents / connectors / jobs"]
    API --> Retrieval["Retrieval plane<br/>query / search / chat"]

    Control --> Postgres[("Postgres<br/>users, metadata, audit")]
    Ingestion --> Files[("Source files<br/>local disk / Google Drive")]
    Ingestion --> Redis[("Redis queue")]
    Redis --> Worker["Ingestion worker"]
    Worker --> Docling["Docling conversion"]
    Docling --> Embeddings["Embedding providers"]
    Embeddings --> Turbopuffer[("Turbopuffer vectors")]

    Retrieval --> Embeddings
    Retrieval --> Turbopuffer
    Retrieval --> LLM["Chat provider"]
    LLM --> Answer["Retrieved answer"]

    classDef surface fill:#fff,stroke:#d4d4d4,color:#111
    classDef api fill:#111,stroke:#111,color:#fff
    classDef plane fill:#f8f8f8,stroke:#b8b8b8,color:#111
    classDef pipeline fill:#fffdf5,stroke:#d6c98a,color:#111
    classDef store fill:#f5f9ff,stroke:#9fb7d9,color:#111
    classDef output fill:#f7fff8,stroke:#9cc9a4,color:#111

    class Clients surface
    class API api
    class Control,Ingestion,Retrieval plane
    class Redis,Worker,Docling pipeline
    class Postgres,Files,Embeddings,Turbopuffer,LLM store
    class Answer output' />

| Component | Purpose | Default Address |
|-----------|---------|-----------------|
| **bigRAG API** | REST API server (FastAPI) | `http://localhost:4000` |
| **Admin UI** | Admin web app | `http://localhost:3000` |
| **PostgreSQL** | Users, sessions, API keys, metadata, audit | `localhost:5432` |
| **Turbopuffer** | Vector storage and semantic, keyword, and hybrid search | Managed service |
| **Redis** | Ingestion + event bus | `localhost:6379` |

## Explore

<Cards>
  <Card title="Installation" href="/docs/getting-started/installation">
    Set up bigRAG on your machine
  </Card>
  <Card title="Quickstart" href="/docs/getting-started/quickstart">
    Upload and search your first document in minutes
  </Card>
  <Card title="Admin UI" href="/docs/admin-ui">
    The admin UI tour — setup, chat, keys, webhooks
  </Card>
  <Card title="Security & Auth" href="/docs/concepts/security">
    Admin accounts, sessions, API keys, audit
  </Card>
  <Card title="API Reference" href="/docs/api-reference/collections">
    Complete REST API documentation
  </Card>
  <Card title="TypeScript SDK" href="/docs/sdks/typescript">
    Zero-dependency client with full type safety
  </Card>
  <Card title="MCP Server" href="/docs/sdks/mcp">
    Plug bigRAG into Claude Desktop, Cursor, or any MCP runtime
  </Card>
  <Card title="Deployment" href="/docs/deployment/docker">
    Deploy with Docker Compose
  </Card>
</Cards>
