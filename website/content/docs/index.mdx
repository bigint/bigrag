---
title: Introduction
description: rag.computer is an open-source, self-hostable RAG platform for document ingestion, vector search, and retrieval-augmented generation.
---

import { Cards, Card } from "fumadocs-ui/components/card";

rag.computer is an open-source RAG (Retrieval-Augmented Generation) platform. It provides a complete pipeline for document ingestion, chunking, embedding, and vector search — behind a REST API, TypeScript / Python / Rust SDKs, an admin UI, and an MCP server for Claude Desktop, Cursor, and any MCP-aware runtime.

## Key Features

- **End-to-end RAG pipeline** — upload documents, auto-chunk, embed, search, and rerank in one platform
- **Any document format** — PDF, DOCX, PPTX, XLSX, HTML, Markdown, CSV/TSV/XML/JSON, and scanned images (OCR) via [Docling](https://github.com/DS4SD/docling)
- **Any embedding model** — OpenAI, Cohere, Voyage, or `openai_compatible` gateways (Ollama, vLLM, TEI, LiteLLM, Azure, Bedrock) configured per collection
- **Three search modes** — semantic, keyword, and hybrid, with optional Cohere reranking
- **Qdrant vector database** — HNSW vector search with tenant-aware payload indexes
- **First-class auth** — admin accounts, session cookies, scoped `ragc_sk_…` API keys, and full audit/access logs
- **Operational features** — batch ingestion, Google Drive sync, webhooks with HMAC signatures, metadata-schema validation, an evaluation runner, and per-collection query analytics
- **Admin UI** — first-run setup, collection browser, persistent chat, connector management, access logs, MCP credentials, and API-key minting
- **Self-hostable** — Docker Compose, no external dependencies
- **MIT licensed** — run it anywhere, forever free

## Architecture

<Mermaid chart='graph TD
    MCP([MCP client / Claude / Cursor]) -->|rag-computer-mcp| API
    AdminUI([Admin UI]) -->|session cookie| API
    SDK([TS / Python / Rust SDK]) -->|ragc_sk_… key| API
    API[rag.computer API]
    API --> Auth[Auth & audit]
    API --> Collections[Collections]
    API --> Documents[Documents]
    API --> Query[Query]
    API --> Chat[Chat]
    API --> Webhooks[Webhooks]
    Documents -->|store files| Storage[(Local disk)]
    Documents -->|sync source files| Drive[Google Drive]
    Documents -->|enqueue| Redis[(Redis)]
    Redis -->|process| Worker[Ingestion worker]
    Worker -->|parse| Docling[Docling]
    Worker -->|embed| Embedding[Embedding provider]
    Worker -->|store vectors| Qdrant[(Qdrant)]
    Query -->|search| Qdrant
    Query -->|embed query| Embedding
    Chat -->|retrieve context| Query
    Chat -->|generate answer| LLM[Chat provider]
    Collections --> Postgres[(Postgres)]
    Documents --> Postgres
    Webhooks --> Postgres
    Auth --> Postgres' />

| Component | Purpose | Default Address |
|-----------|---------|-----------------|
| **rag.computer API** | REST API server (FastAPI) | `http://localhost:4000` |
| **Admin UI** | Admin web app | `http://localhost:3000` |
| **PostgreSQL** | Users, sessions, API keys, metadata, audit | `localhost:5432` |
| **Qdrant** | Vector storage and search | `localhost:6333` |
| **Redis** | Ingestion + event bus | `localhost:6379` |

## Explore

<Cards>
  <Card title="Installation" href="/docs/getting-started/installation">
    Set up rag.computer on your machine
  </Card>
  <Card title="Quickstart" href="/docs/getting-started/quickstart">
    Upload and search your first document in minutes
  </Card>
  <Card title="Admin UI" href="/docs/admin-ui">
    The admin UI tour — setup, chat, keys, webhooks
  </Card>
  <Card title="Security & Auth" href="/docs/concepts/security">
    Admin accounts, sessions, API keys, audit
  </Card>
  <Card title="API Reference" href="/docs/api-reference/collections">
    Complete REST API documentation
  </Card>
  <Card title="TypeScript SDK" href="/docs/sdks/typescript">
    Zero-dependency client with full type safety
  </Card>
  <Card title="MCP Server" href="/docs/sdks/mcp">
    Plug rag.computer into Claude Desktop, Cursor, or any MCP runtime
  </Card>
  <Card title="Deployment" href="/docs/deployment/docker">
    Deploy with Docker Compose
  </Card>
</Cards>
