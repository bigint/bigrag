[project]
name = "bigrag"
version = "2026.4.30"
description = "Self-hostable RAG platform with Docling ingestion and Milvus vector DB"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0,<1",
    "starlette>=0.40.0,<1",
    "uvicorn[standard]>=0.34.0,<1",
    "asyncpg>=0.30.0,<1",
    "sqlalchemy[asyncio]>=2.0.36,<3",
    "alembic>=1.14.0,<2",
    "pymilvus>=2.5.0,<3",
    "docling>=2.25.0,<3",
    "huggingface-hub>=0.36.0,<2",
    "openai>=1.60.0,<2",
    "cohere>=5.13.0,<6",
    "pydantic[email]>=2.10.0,<3",
    "pydantic-settings>=2.7.0,<3",
    "python-multipart>=0.0.18,<1",

    "httpx>=0.28.0,<1",
    "tomli>=2.2.0,<3",
    "redis[hiredis]>=5.2.0,<8",
    "orjson>=3.10.0,<4",
    "structlog>=24.4.0,<26",
    "aiobotocore>=2.18.0,<3",
    "botocore>=1.35.0,<2",
    "argon2-cffi>=23.1.0,<26",
    "cryptography>=44.0.0,<48",
    "mcp>=1.12.0,<2",
    "numpy>=2.0.0,<3",
    "tiktoken>=0.8.0,<1",
]

[dependency-groups]
dev = ["pytest>=8.0.0", "ruff>=0.8.0"]

[project.scripts]
bigrag = "bigrag.main:cli"
bigrag-mcp = "bigrag.mcp_server:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bigrag"]

[tool.hatch.build.targets.wheel.force-include]
"alembic" = "bigrag/_alembic"
"alembic.ini" = "bigrag/_alembic/alembic.ini"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "T201", "FA102"]
ignore = ["B008"]  # Depends() and Query() in FastAPI defaults are intentional

[tool.deptry.package_module_name_map]
argon2-cffi = "argon2"
python-multipart = "python_multipart"

[tool.deptry.per_rule_ignores]
DEP002 = ["python-multipart"]
